# test/bats/acceptance/ (reserved)

Reserved: **Acceptance** level (UAT / OAT). ISTQB taxonomy,
ADR-00000018 -- verifies what the downstream consumer receives (the
scaffolded framework + its UX: `just` commands, help, completion, the
generated layout), from the consumer's chair.

Populated in S5 (#785). Until then this directory is an intentional
empty placeholder (`.gitkeep`) so the full level x type grid is visible
in the tree.
