#!/usr/bin/env bash
# Companion script for the `__NAME__` command group (scaffolded by
# `just template new`). Replace this with your own logic.
set -euo pipefail

main() {
  echo "hello from the __NAME__ group"
}

main "$@"
